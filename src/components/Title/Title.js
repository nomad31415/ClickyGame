import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>The Clicky Game!</h1>
        <p>Click on an image to earn points, but do not click on same more than once!</p>
    </div>
);

export default Title;

